{
	"name": "backend",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node index.js",
		"dev": "nodemon index.js",
		"build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../notesExample && npm run build && @powershell Copy-Item dist -Recurse -Force ../backend",
		"deploy:full": "npm run build:ui && git add . && git commit -m Deployment && git push"
	},
	"author": "Lucas Gomes",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"express": "^4.18.2",
		"morgan": "^1.10.0"
	},
	"devDependencies": {
		"nodemon": "^2.0.22"
	}
}
